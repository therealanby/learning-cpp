#include <iostream>

//using namespace std; //the location of this will determine the scope in which this namespace will be used
//better practice to use "using std::cout;" as it will only import count from std instead of a bunch of stuff
int main()
{
    //using namespace std;
    std::cout << "Hello World\n";
    //cout<< ""; if you use "using namespace std;"
    //return 0; optional
    // g++ filename.cpp to compile
    // ./a.out to run (in vscode terminal)
    //in normal console, just type the name (you can exclude the ./)
}