#include <iostream>

using std::cout;

int main(){
    int apples = 5; //type integer. structure: [type] [identifier] [assignment op] [value];
    //delcaration (declaring an int variable called apples) and initialization (giving a value to that variable)
    int pears;
    pears = 10;
    int fruits = pears + apples;
    cout << fruits;
}
