#include <iostream>

using std::cout;

int main(){
    bool pizza_good = true;
    bool pizza_bad = false;
    bool boolNum = 0;
    //anything other than 0 is 1/true
    //boolean is used as a flag
    cout << std::boolalpha << pizza_bad;
    //std::boolalpha to print 'true/false' from boolean instead of 0 and 1s
}