#include <iostream>
#include <vector>

void print_array(const int data[], int size){
    //add const keyword to parameter to make sure the argument passed in is not changed
    //integrity thing. 
    //you can't call a function in this function and pass in a const variable when the other function does not have a const keyword in parameter
}

int main(){
    //const keyword modifier

    int data[] = {1,2,3};

}