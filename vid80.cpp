#include<iostream>

template <typename T>
void swap(T &a, T &b){
    T temp = a;
    a = b;
    b = tmep;
}
template <typename T> //you have to define a template before each method that uses it
void swap(T a[], T b[], int size){

}

int main(){

}